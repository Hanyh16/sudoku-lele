/**
 * 策略基类，定义了策略的接口规范
 * 所有具体的策略实现都应该继承自此类
 */
export class BaseStrategy {
  /**
   * 检查是否满足策略应用的前置条件
   * @param {Array} grid - 当前数独网格
   * @returns {boolean} - 是否满足策略应用条件
   */
  preCondition(grid) {
    throw new Error("Base strategy is just an interface.");
  }

  /**
   * 应用策略到网格
   * @param {Array} grid - 当前数独网格
   * @param {Array} strategyApplyCell - 记录应用策略的单元格位置数组
   */
  apply(grid, strategyApplyCell) {
    throw new Error("Base strategy is just an interface.");
  }

  /**
   * 获取策略的描述信息
   * @returns {string} - 策略描述
   */
  strategyDescription() {
    throw new Error("Base strategy is just an interface.");
  }
}

